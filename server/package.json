{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "author": "",
  "license": "ISC",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc",
    "prestart": "npm run build",
    "server": "NODE_ENV=development node .",
    "start": "cd ..; npm run build; cd server; NODE_ENV=development && open http://localhost:3010 && npm run server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@types/dotenv-flow": "^3.1.0",
    "cambrian-base": "^1.0.145",
    "dotenv-flow": "^3.2.0",
    "express": "^4.17.1",
    "compression": "^1.7.4",
    "@types/compression": "^1.7.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.11",
    "@types/node": "^14.14.22",
    "concurrently": "^5.3.0",
    "tslint": "^6.1.3",
    "typescript": "^4.1.3"
  }
}
